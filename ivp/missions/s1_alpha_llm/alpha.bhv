//--------    FILE: alpha.bhv  (s1_alpha_llm)   -------------
//
// Blank mission: no pre-programmed route. Nothing here moves the
// boat until the operator or pLLMAgent posts one of these:
//
//   goto / route : DEPLOY=true, RETURN=false, WPT_UPDATE=points=...
//   return       : DEPLOY=true, RETURN=true
//   stop         : DEPLOY=false
//   speed        : WPT_UPDATE=speed=..., RETURN_UPDATE=speed=...
//
// Each behavior clears DEPLOY when it finishes, so the helm idles
// (all-stop, boat coasts to a halt) until the next command.

initialize   DEPLOY = false
initialize   RETURN = false

//----------------------------------------------
Behavior = BHV_Waypoint
{
  name      = waypt_goto
  pwt       = 100
  condition = DEPLOY = true
  condition = RETURN = false
  endflag   = DEPLOY = false
  endflag   = MISSION = goto_complete
  updates   = WPT_UPDATE
  perpetual = true

  points         = empty     // idle until a WPT_UPDATE arrives
  speed          = 2.0       // m/s, changed by WPT_UPDATE=speed=...
  lead           = 8
  lead_damper    = 1
  lead_to_start  = true
  capture_line   = true
  capture_radius = 5.0
  slip_radius    = 15.0
  order          = normal
  repeat         = 0

  visual_hints = nextpt_color=yellow, nextpt_vertex_size=8, nextpt_lcolor=gray70
  visual_hints = vertex_color=dodger_blue, edge_color=white
  visual_hints = vertex_size=5, edge_size=1
}

//----------------------------------------------
Behavior = BHV_Waypoint
{
  name      = waypt_return
  pwt       = 100
  condition = DEPLOY = true
  condition = RETURN = true
  endflag   = RETURN = false
  endflag   = DEPLOY = false
  endflag   = MISSION = return_complete
  updates   = RETURN_UPDATE
  perpetual = true

  points         = 0,-20     // launch position (uSimMarineV22 start_pos)
  speed          = 2.0
  lead           = 8
  lead_damper    = 1
  lead_to_start  = true
  capture_line   = true
  capture_radius = 3.0
  slip_radius    = 10.0
}
